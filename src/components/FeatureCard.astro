---
const { title = await Astro.slots.render('title'), image = await Astro.slots.render('image') } = Astro.props;
---

<div class="bg-white rounded-2xl p-8 shadow-sm hover:shadow-md transition-all border border-gray-200">
  <div class="h-24 flex items-center justify-center mb-6">
    {image}
  </div>
  <h3 class="text-xl font-semibold text-center text-gray-900">
    {title}
  </h3>
</div>
